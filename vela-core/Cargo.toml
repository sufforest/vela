[package]
name = "vela-core"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Vela's Matrix protocol primitives: events, state resolution, auth rules, federation crypto."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
base64 = { workspace = true }
ed25519-dalek = { workspace = true }
rand = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
# Linear-time (RE2-style) regex — used only for glob `content.body` push-rule
# patterns, where an attacker-set pattern must not cause catastrophic
# backtracking on the /sync eval hot path.
regex = { workspace = true }

[lints]
workspace = true
